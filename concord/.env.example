# URL do backend que emite os tokens do LiveKit.
# Em desenvolvimento local, o padrão já aponta para http://localhost:3001.
# Em produção (Render), aponte para a URL pública do seu serviço de backend.
VITE_API_URL=http://localhost:3001
